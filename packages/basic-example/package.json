{
  "name": "basic-aube-app",
  "version": "0.0.1",
  "description": "sample aube app",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && node build/index.js",
    "start:dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gaston Che",
  "license": "ISC",
  "dependencies": {
    "@aube/core": "*",
    "@aube/express": "*"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1"
  }
}
